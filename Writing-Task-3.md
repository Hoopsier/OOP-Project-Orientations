# Orientation 3

## 1

I would create a static list of ages for the Student class, adding ages randomly,
using a custom distribution.
If there is a need to shove it into a graph,
I'll print them to a `data.csv` file with `,` as separator.

## 3

A singleton cannot be made with new,
because singletons are single instance objects.
They should only have the one instance,
so there is no point in even giving the possibility to make another.

## 4

I have thus familiarized myself with normal distribution in real life
[https://www.statology.org/example-of-normal-distribution/],
exponential distribution, which is used for delta 20
[https://www.statology.org/exponential-distribution-real-life-examples/]
